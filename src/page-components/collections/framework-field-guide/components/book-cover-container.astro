<div class="bookCoverContainer">
	<slot />
</div>

<style>
	.bookCoverContainer {
		max-height: min(var(--max-width_small), 100vw);
		height: 100vh;
		position: relative;
		z-index: -1;
		/* Just to be safe */
		margin-bottom: -4px;
	}
</style>
