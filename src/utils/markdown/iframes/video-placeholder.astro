---
import {
	VideoPlaceholder as PreactVideoPlaceholder,
	VideoPlaceholderProps,
} from "components/video/video-placeholder";

export type Props = VideoPlaceholderProps;
---

<PreactVideoPlaceholder {...Astro.props} client:load />
