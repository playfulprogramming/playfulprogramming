---
import {
	FileList as PreactFileList,
	Directory,
	File,
} from "components/file-list/file-list";

export interface Props {
	items: Array<Directory | File>;
}
---

<PreactFileList items={Astro.props.items} />
