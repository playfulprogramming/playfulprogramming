---
import type { QuizRadioInlineProps } from "#components/quiz-radio/quiz-radio-inline.tsx";
import { QuizRadioInline as PreactQuizRadio } from "#components/quiz-radio/quiz-radio-inline.tsx";
import type { PlayfulNode } from "../components.ts";
import Content from "../content.astro";
import { getPrefixLanguageFromPath } from "#utils/translations.ts";

export type Props = Omit<QuizRadioInlineProps, "locale"> & {
	children: PlayfulNode[];
};
const { children, ...props } = Astro.props;
const locale = getPrefixLanguageFromPath(Astro.url.pathname);
---

{
	children.length ? (
		<PreactQuizRadio {...props} locale={locale} client:load>
			<Content content={children} />
		</PreactQuizRadio>
	) : (
		<PreactQuizRadio {...props} locale={locale} client:load />
	)
}
