---
// TODO: Add click back to `li`
// TODO: Make user-profile-pic clickable again
import { PostCardProps, getPostCardHTML } from "./get-post-card-html";

const postCardHTML = getPostCardHTML(Astro.props as PostCardProps);
---

<Fragment set:html={postCardHTML} />
