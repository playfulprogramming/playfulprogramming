{
  "name": "e2e",
  "version": "3.0.505.4-alpha-beta-sigma-23893031394892",
  "private": true,
  "description": "E2E tests for playfulprogramming",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:ci": "CI=true playwright test --reporter=github",
    "docker:build": "docker compose -f ./docker-compose.e2e.yml build e2e",
    "docker:test": "docker compose -f ./docker-compose.e2e.yml run --rm e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0"
  }
}
